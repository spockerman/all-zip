postgres:
  username: ${DATABASE_POSTGRES_USER}
  password: ${DATABASE_POSTGRES_PASSWORD}
  schema: zip-code-db
  url: ${DATABASE_POSTGRES_HOST}
  port: ${DATABASE_POSTGRES_PORT}

server:
  port: 9191
  undertow:
    threads:
      worker: 34
      io: 2
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none